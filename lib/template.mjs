// ────────────────────────────────────────────────
// テンプレート展開（プロジェクト新規作成）
// ────────────────────────────────────────────────
import { SCHEMA_VERSION } from './constants.mjs'

export const EMBEDDED_TEMPLATE = {
  'VERSION.md': ({ projectName, today }) => `---
schema: ${SCHEMA_VERSION}
project_name: ${projectName}
created: ${today}
---

# Issue Manager 対応プロジェクト

このプロジェクトは issue_manager で管理されています。

- スキーマバージョン: \`${SCHEMA_VERSION}\`
- 管理画面: \`issue_manager/start.bat\` を実行してブラウザで開く
- ファイル直編集も可（エディタで編集している間は管理画面からの保存はロックされます）
`,

  'RULES.md': ({ projectName }) => `# プロジェクト運用ルール

## プロジェクト概要
${projectName}（目的を1-3行で記載）

## 目的（この運用の）
チケットファイル制により、セッション間の引継ぎをスムーズにし、
トークン消費を抑えながら作業を継続可能にする。

## フォルダ構成
\`\`\`
tickets/
├── VERSION.md          # issue_manager 対応マーカー
├── RULES.md            # このファイル（運用ルール）
├── INDEX.md            # アクティブチケット一覧
├── TICKET_TEMPLATE.md  # チケット雛形
├── inbox/              # 未整理の入力（アイデア・報告・気付き）
├── todo/               # 精査済み・着手可能
├── doing/              # 着手中
├── blocked/            # ブロック中（外部待ち・判断待ち）
├── done/               # 完了済み
├── archive/            # 退避済み（見えなくするだけ、復元可能）
└── .trash/             # ソフト削除退避先
\`\`\`

## チケットファイル名規則
形式: \`YYYYMMDD_P<優先度>_<工程フェーズ>.md\`
例: \`20260423_P0_設計.md\`, \`20260424_P1_実装_API.md\`

優先度:
- P0: 最優先（ブロッカー、緊急対応）
- P1: 高（今週中）
- P2: 中（通常タスク）
- P3: 低（余裕があれば）

## ステータス更新ルール（イベント駆動）
- 一会話ごとには更新しない
- inbox → todo: 精査済み（優先度・完了条件・依存関係が届き、セッション開始時に一括整理して昇格）
- todo → doing: セッション開始時に着手チケットを1つ選んで移動
- doing → blocked: 外部待ち・判断待ちが発生したら即移動＋理由追記
- doing → done: 完了条件を全て満たした時点で移動（done 移動時に \`- 完了日\` が自動追記される）
- セッション終了時: 作業ログに引継ぎメモを1-3行追記

## チケットの日付メタ（起票日・完了日）
- **作成日**: チケット起票時に \`- 作成日: YYYY-MM-DD\` を記入
- **完了日**: done へ移動した時点で自動的に \`- 完了日: YYYY-MM-DD\` が本文に挿入される
- これらの日付はエクスポート機能でタイムライン管理に活用される

## エクスポート機能
- 画面右上の「⬇ エクスポート」でチケット一覧を以下の形式で出力可能
  - **TSV**: Excel 直開き用（BOM 付き UTF-8、タブ区切り、CRLF）
  - **JSON**: タイムラインUI連携用
- 出力列: file / lane / title / priority / status / archivedFrom / createdAt / completedAt / mtime / ctime / path

## チケット本体の「ステータス」欄の値
- レーン名（\`todo\` / \`doing\` / \`blocked\` / \`done\`）: 通常はこれ
- \`進行中\`: doing に複数チケットがある時、今セッションで実際に作業している方につける（緑バッジ）
- \`中断\`: doing にあるが別作業を先行中のチケットにつける（オレンジバッジ）
- \`実機検証待ち\` / \`ユーザー確認待ち\` など任意のステータス（グレーバッジ）

## INBOX 運用
- アイデア・報告・気付きはすべて inbox/ に投入
- セッション開始時に todo へ昇格、.trash へ廃棄、inbox に留保のいずれかへ振り分け

## ARCHIVE 運用
- 見えなくしたいが削除はしたくないチケットは archive/ へ退避
- 本文に \`- archive元: <元レーン>\` が自動記録され、後で元のレーンに戻せる
- 画面では既定で非表示。「📦 archive を表示」で表示切替

## STATUS.md について（自動生成・編集禁止）
- \`tickets/STATUS.md\` は **サーバーが自動生成**する状態スナップショット
- 生成タイミング: サーバー起動時 + mutation API 成功時
- **手で編集しない**。次回 mutation 時に上書きされる
- 含まれる情報: 最終更新時刻 / 各レーン現状 / 直近 done 7件 / 直近 archive 7件 / 統計
- \`.gitignore\` で除外推奨（リポジトリ管理不要）
- INDEX.md との役割分担: STATUS.md（機械生成） / INDEX.md（人・Claude の推奨実行順・引継ぎメモ）

## 作業ログの肥大化対策
- チケット内の作業ログは2層構造（サマリ欄＋詳細ログ欄）
- 10ターン分程度溜まったらサマリ欄に1行要約を追加
- 詳細ログ欄の原文は削除・改変しない（追記のみ）

## 課題対応の割り込み方針
- パターンA: 別チケット化して本流を続行
- パターンB: 本流をスタックして課題対応を先行
- パターンC: 本流に組み込む
- doing は原則 1 つ、例外時は最大 3 つまで

## セッション開始/終了手順
開始: RULES.md と INDEX.md を読む → todo/ → doing/ へ移動 → 作業開始
終了: サマリ追記 → 詳細ログ追記 → 引継ぎメモ → done/ へ移動 + INDEX 更新

## Windows BAT ファイル編集ルール
\`.bat\` を Claude が生成・更新したら、必ず BOM 修復ツールを通すこと。
\`@echo off\` 直後に \`chcp 65001 >nul\`、表示メッセージは英語中心。

## プロジェクト固有ルール
<このプロジェクト特有の運用ルールがあればここに追記>
`,

  'INDEX.md': ({ projectName, today }) => `# アクティブチケット一覧

プロジェクト: ${projectName}
最終更新: ${today}

## 推奨実行順（依存解決済み・上から着手）
_なし_

## doing（着手中）
_なし_

## blocked（ブロック中）
_なし_

## todo（精査済み・優先度順）
_なし_

## inbox（未整理の入力）
_なし_

## 直近done（参考、最新5件）
_なし_

---
## メモ
- 推奨実行順はセッション終了時に更新
- 優先度P0から順に着手
- 新規追加指示は inbox/ へ、精査後に todo/ へ昇格
- 完了チケットは done/ へ移動しINDEXから削除
`,

  'TICKET_TEMPLATE.md': () => `# <チケットタイトル>

- チケットID: YYYYMMDD_P<優先度>_<工程フェーズ>
- 作成日: YYYY-MM-DD
- ステータス: todo

## 目的
<1-2行で何のためのタスクか>

## 前提・依存
- 依存チケット: <あれば記載、なければ「なし」>
- 参照ファイル: <仕様書パスなど>

## 完了条件
- [ ] 条件1
- [ ] 条件2
- [ ] 条件3

## 作業ログ

### サマリ(最新が上、1行要約)

### 詳細ログ(原文、時系列、削除改変禁止)

## 引継ぎメモ

<!--
完了日は done へ移動する時に自動で1行追加される:
- 完了日: YYYY-MM-DD
-->
`,

  'README.md': ({ projectName }) => `# ${projectName} — チケット運用

このディレクトリは issue_manager で管理されるチケット領域です。

## フォルダの意味

| フォルダ | 意味 |
|----------|------|
| inbox/   | 未整理の入力 |
| todo/    | 精査済み・着手可能 |
| doing/   | 着手中 |
| blocked/ | ブロック中 |
| done/    | 完了済み |
| archive/ | 退避済み（復元可能） |
| .trash/  | ソフト削除退避先 |
`,
}