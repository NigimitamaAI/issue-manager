{
  "_comment": "issue_manager instance config. Change port when running multiple instances on the same PC. CLI args override this file.",
  "_comment_format": "Since v1.1, environment-variable expansion syntax is not used. Write values directly.",
  "port": 5181,
  "_comment_port": "Server port number. Default is 5180. Use another port for multiple instances.",
  "root": "G:/codex",
  "_comment_root": "Root directory for project search. On Windows, forward slashes are recommended. If blank, the parent directory of issue_manager is used.",
  "nodeExe": "E:/act_app/nodejs/node.exe",
  "_comment_nodeExe": "Node executable used by start.bat. Use node if it is available in PATH. Full path example: E:/act_app/nodejs/node.exe",
  "bomFixerPath": "G:/codex/_tools/ファイル操作所_v2.bat",
  "_comment_bomFixerPath": "Path to a tool that strips UTF-8 BOM from .bat files. Leave blank if not used.",
  "logDir": "logs",
  "_comment_logDir": "Directory for debug logs. Relative paths are resolved from the issue_manager folder.",
  "aiName": "codex",
  "_comment_aiName": "AI identifier for this server instance (Phase 2-A). Used to write .issuemgr/ai-<aiName>.json. Allowed: alphanumerics, hyphens, underscores. Empty string disables ai-state feature. CLI --ai-name overrides this.",
  "projectName": "",
  "roots": [
    {
      "id": "default",
      "label": "default",
      "type": "local",
      "path": "G:\\codex",
      "capabilities": [
        "read",
        "browse",
        "createProject",
        "importProject",
        "writeTickets",
        "writeMeta",
        "openExternal"
      ],
      "writeScopes": [
        "tickets",
        ".issuemgr"
      ]
    },
    {
      "id": "G_dev",
      "label": "G_dev",
      "type": "local",
      "path": "G:\\dev",
      "capabilities": [
        "read",
        "browse",
        "createProject",
        "importProject",
        "writeTickets",
        "writeMeta",
        "openExternal"
      ],
      "writeScopes": [
        "tickets",
        ".issuemgr"
      ]
    }
  ]
}
